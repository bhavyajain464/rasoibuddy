/** Kitchmate brand assets and copy — single source for logo paths. */
export const BRAND_NAME = 'Kitchmate';
export const BRAND_MOTTO = 'Less waste. Smarter meals. Calmer evenings.';

/** Login inset header (transparent logo sits on this). */
export const BRAND_HEADER_BG = '#FFFFFF';

/** `assets/logo.png` width ÷ height (1080×840 transparent PNG). */
export const BRAND_LOGO_ASPECT = 1080 / 840;

export const brandLogo = require('../../assets/logo.png');
